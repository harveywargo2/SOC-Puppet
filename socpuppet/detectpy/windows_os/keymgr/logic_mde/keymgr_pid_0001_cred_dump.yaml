---
title: keymgr_pid_0001_cred_dump
intel:
  description:
    - rundll32 using key manager to dump creds
  detectType: single_line_match
  detectCon: 2
  detectLanguage: kql
  tags:
    - lolbin
  attackTechnique: t1003
  attackSoftware:
  references:
  emulation:
    - atomic_red_team_t1003_t6_guid_84113186-ed3c-4d0d-8a3c-8980c86c1f4a
  pattern:
    - rundll32.exe keymgr,KRShowKeyMgr
dataRequirements:
  dataSource: windows_os
  dataType: process_creation
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_process_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: |
    | where ProcessCommandLine has 'keymgr' 
      and ProcessCommandLine has 'krshowkeymgr'
    | where ProcessCommandLine contains 'rundll'