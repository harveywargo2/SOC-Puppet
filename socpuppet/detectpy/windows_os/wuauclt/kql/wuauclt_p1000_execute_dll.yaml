---
title: executing_dll
pid: wuauclt_p1000
description: wuauclt executing dll
metaData:
  attackTechnique: t1218
  attackSoftware:
  detectUseCase:
  detectType: single_line_match
  detectCon: 3
  platform: windows_os
  dataFlow:
    - msft defender endpoint or m365d agent <on>
    - any windows os <logs>
    - process creation events <forwards to>
    - DeviceProcessEvents <data table>
  tags:
    - lol
kqlDetect:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where FileName =~ 'wuauclt.exe'
    | where ProcessCommandLine has_all ('.dll', '/UpdateDeploymentProvider', 'RunHandlerComServer')
    | where not (ProcessCommandLine has_any('UpdateDeploymentProvider.dll' , 'UpdateDeploy.dll' , 'wuaueng.dll'))