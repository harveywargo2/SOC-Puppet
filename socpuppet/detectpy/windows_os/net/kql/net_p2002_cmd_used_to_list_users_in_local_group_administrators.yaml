---
title: cmd_used_to_list_users_in_local_group_administrators
pid: net_p2002
description:
  - net.exe command line tool can be used to discover users in administrators local group
  - see net_references.md
metaData:
  attackTechnique: t1069.001
  attackSoftware: s0039
  detectUseCase:
  detectType: single_line_match
  detectCon: 3
  platform: windows_os
  dataFlow:
    - msft defender endpoint or m365d agent <on>
    - any windows os <logs>
    - process creation events <forwards to>
    - DeviceProcessEvents <data table>
  tags:
    - lol
kqlDetect:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: |
    | where FileName has_any ('net', 'net1')
    | where ProcessCommandLine has_any ('localgroup', 'localgroups')
    | where ProcessCommandLine has ('administrators')