---
title: cmd_used_to_stop_service
pid: net_p5000
description:
  - net.exe command line tool can be used to stop services on device
  - see net_references.md
metaData:
  attackTechnique:
  attackSoftware: s0039
  detectUseCase:
  detectType: single_line_match
  detectCon: 4
  platform: windows_os
  dataFlow:
    - msft defender endpoint or m365d agent <on>
    - any windows os <logs>
    - process creation events <forwards to>
    - DeviceProcessEvents <data table>
  tags:
    - lol
kqlDetect:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: |
    | where FileName has_any ('net', 'net1')
    | where ProcessCommandLine has_all ('stop')