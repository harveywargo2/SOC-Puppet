---
title: reg_pid_0004_hklm_lsa_secrets_save
intel:
  description:
    - reg.exe used to dump LSA secrets
    - LSA secrets are stored in the registry
  detectType: single_line_match
  detectCon: 1
  detectLanguage: kql
  tags:
    - lolbin
  attackTechnique: t1003.001
  attackSoftware:
  references:
  emulation:
    - atomic_red_team_t1003_004_t1_guid_55295ab0-a703-433b-9ca4-ae13807de12f
  pattern:
    - reg save HKLM\security\policy\secrets <path to dump file>
dataRequirements:
  dataSource: windows_os
  dataType: process_create
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_file_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where ProcessCommandLine has 'reg'
      and ProcessCommandLine has 'hklm' 
      and ProcessCommandLine has 'save'
    | where ProcessCommandLine contains 'security'