---
title: reg_pid_0005_hklm_create_minint
intel:
  description:
    - The presence of MiniNT Registry key causes various Windows components to think they’re running in the Windows Preinstallation Environment.
    - Prevents windows event logs from working
  detectType: single_line_match
  detectCon: 3
  detectLanguage: kql
  tags:
    - lolbin
  attackTechnique: t1562.002
  attackSoftware:
  references:
    - https://www.quppa.net/blog/2016/04/14/beware-of-the-minint-registry-key/
    - https://securitydatasets.com/notebooks/atomic/windows/defense_evasion/SDWIN-220803205800.html
  emulation:
  pattern:
    - reg createkey -k 'HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\MiniNt'
    - REG ADD HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\MiniNt
dataRequirements:
  dataSource: windows_os
  dataType: process_create
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_file_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where ProcessCommandLine has 'reg'
    | where ProcessCommandLine has 'SYSTEM' 
      and ProcessCommandLine has 'Control'
    | where ProcessCommandLine contains @'\Control\MiniNt'
    | where ProcessCommandLine has 'createkey'
      or ProcessCommandLine has 'add'