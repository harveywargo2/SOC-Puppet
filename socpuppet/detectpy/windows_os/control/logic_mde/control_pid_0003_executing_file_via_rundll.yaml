---
title: control_pid_0002_executing_dll_file
intel:
  description:
    - abusing control.exe to execute dll file
  detectType: single_line_match
  detectCon: 5
  detectLanguage: kql
  tags:
    - lolbin
  attackTechnique: t1218.002
  attackSoftware:
  references:
  emulation:
    -
  pattern:
    -
dataRequirements:
  dataSource: windows_os
  dataType: process_creation
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_process_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where InitiatingProcessFileName has 'control'
      and FileName startswith 'rundll'
    | where ProcessCommandLine contains 'shell32.dll'
      and ProcessCommandLine contains 'Control_Rundll'