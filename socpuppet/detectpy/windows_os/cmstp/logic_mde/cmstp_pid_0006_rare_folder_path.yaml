---
title: cmstp_pid_0006_rare_folder_path
intel:
  description:
    - rare activity of cmstp.exe being called from any folder that isn't system32
  detectType: single_line_match
  detectCon: 3
  detectLanguage: kql
  tags:
    - lolbin
  attackTechnique: t1036
  attackSoftware:
  references:
  emulation:
    -
  pattern:
    -
dataRequirements:
  dataSource: windows_os
  dataType: process_creation
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_process_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where FileName has 'cmstp'
      or ProcessVersionInfoOriginalFileName has 'cmstp'
    | where FolderPath !contains @'c:\windows\system32\'