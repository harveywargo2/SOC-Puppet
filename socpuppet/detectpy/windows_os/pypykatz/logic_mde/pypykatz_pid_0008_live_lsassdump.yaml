---
title: pypykatz_pid_0006_live_lsassdump
intel:
  description:
    - pypykatz live smb command
    - Dumps and parses the LSASS remotely over SMB
    - Connection is set up using the current user's context.
    - LSASS dump file will be deleted after command finishes (best effort)
  detectType: single_line_match
  detectCon: 2
  detectLanguage: kql
  tags:
    - python
  attackTechnique: t1003.001
  attackSoftware:
  references:
  emulation:
  pattern:
dataRequirements:
  dataSource: windows_os
  dataType: process_creation
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_process_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where ProcessCommandLine has 'live'
      and ProcessCommandLine has 'smb'
      and ProcessCommandLine has 'lsassdump'