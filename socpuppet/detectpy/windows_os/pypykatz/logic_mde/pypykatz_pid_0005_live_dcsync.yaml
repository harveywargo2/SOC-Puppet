---
title: pypykatz_pid_0005_live_dcsync
intel:
  description:
    - pypykatz dcsynce attack
    - Performs DCSYNC attack, extracts all hashes and kerberos keys from the domain controller using DRSUAPI
  detectType: single_line_match
  detectCon: 2
  detectLanguage: kql
  tags:
    - python
  attackTechnique:
    - t1003.006
    - t1021.002
  attackSoftware:
  references:
  emulation:
  pattern:
dataRequirements:
  dataSource: windows_os
  dataType: process_creation
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_process_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where ProcessCommandLine has 'live'
      and ProcessCommandLine has 'smb'
      and ProcessCommandLine has 'dcsync'