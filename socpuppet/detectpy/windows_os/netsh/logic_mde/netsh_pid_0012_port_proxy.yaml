---
title: netsh_pid_0012_port_proxy
intel:
  description:
    - netsh used to set up port forwarding port porxy
  detectType: single_line_match
  detectCon: 2
  detectLanguage: kql
  tags:
    - lolbin
  attackTechnique: t1090.001
  attackSoftware: s0108
  references:
  emulation:
    - atomic_red_team_t1090_001_t2_guid_b8223ea9-4be2-44a6-b50a-9657a3d4e72a
  pattern:
    - netsh interface portproxy add v4tov4 listenport=8001 listenaddress=<JUMP BOX IP> connectport=3389 connectaddress=<DESTINATION IP>
dataRequirements:
  dataSource: windows_os
  dataType: process_creation
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_process_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: |
    | where FileName has 'netsh'
    | where ProcessCommandLine has 'portproxy'
      and ProcessCommandLine has 'add'
      and ProcessCommandLine contains 'v4tov4'