---
detect:
  title: powerview_pid_0004_invoke_kerberoast_module
  description:
    - powerview
    - invoke kerberoast
  detectType: single_line_match
  detectLanguage: kql
  detectCon: 1
  urgency: rapid
  inTheWild: common
  noiseProjection: zero
  regressionVolume: only attack pattern match
  responseEffort: low
  confidence: certain
intel:
  tags:
    -
  attackId:
    - t1059
  references:
    - ref_powersploit_powerview.md
  emulation:
    -
  pattern:
    -
dataRequirements:
  dataSource:
    - windows_os
  dataType:
    - powershell_command
  dataCollector: m365d_agent
  dataForwardTo: m365d_security_center
  dataParser:
  dataStorage: m365d_security_center
  dataTable: device_events
  dataIndex:
  dataAccess: api_advanced_hunting
  dataMap:
logic:
  kqlVar:
  kqlTable: DeviceEvents
  kqlQuery: | 
    | where ActionType =~ 'PowerShellCommand'
    | where AdditionalFields has_all ('invoke', 'kerberoast')
      or InitiatingProcessCommandLine has_all ('invoke', 'kerberoast')