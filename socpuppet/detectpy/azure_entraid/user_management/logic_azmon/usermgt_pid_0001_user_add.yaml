---
title: usermgt_pid_0001_user_add
intel:
  description:
    - Identifies accounts that were successfully created
  detectType: single_line_match
  detectCon: 5
  detectLanguage: kql
  tags:
    -
  attackTechnique:
    - t1136
  attackSoftware:
  references:
  emulation:
  pattern:
dataRequirements:
  dataSource: entra_id
  dataType:
    - core_directory
  dataCollector: azure_active_directory
  dataForwardTo: msft_sentinel
  dataParser:
  dataStorage: azure_monitor
  dataTable: audit_logs
  dataIndex: user_management
  dataAccess: api_azure_monitor
  dataMap:
logic:
  kqlVar:
  kqlTable: AuditLogs
  kqlQuery: |
    | where Category =~ 'UserManagement'
      and OperationName =~ 'Add user'
      and Result =~ 'success'