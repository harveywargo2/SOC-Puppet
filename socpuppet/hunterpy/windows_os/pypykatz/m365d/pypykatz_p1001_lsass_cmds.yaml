---
title: lsass_cmds
pid: pypykatz_p1001
description: pypykatz lsass dump commands
metaData:
  attackTechnique: t1003.001
  attackSoftware:
  detectUseCase:
  detectType: single_line_match
  detectCon: 1
  platform: windows_os
  dataFlow:
    - msft defender endpoint or m365d agent <on>
    - any windows os <logs>
    - process creation events <forwards to>
    - DeviceProcessEvents <data table>
  tags:
kqlDetect:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: |
    | where ProcessCommandLine has_all ('cmd' , 'live' , 'lsa')
      or ProcessCommandLine has_all ('python' , 'live' , 'lsa')
      or ProcessCommandLine has_all ('live' , 'registry')