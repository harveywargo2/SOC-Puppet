---
title: pcwdiag_executing_xml
pid: msdt_p1001
description: MSDT executing a xml file
metaData:
  attackTechnique: t1218
  attackSoftware:
  detectUseCase:
  detectType: single_line_match
  detectCon: 3
  platform: windows_os
  dataRequirements:
    dataScope: endpoint_windows_os
    dataCollector: m365d_agent
    dataName: process_creation
    dataForwardTo: m365d_security_center
    dataAccess: m365d_advanced_hunting_api
    dataStorage: device_process_events
    dataMap: device_process_events
  tags:
    - lol
    - cve-2022-30190
kqlDetect:
  kqlVar:
  kqlTable: DeviceProcessEvents
  kqlQuery: | 
    | where FileName =~ 'msdt.exe'
    | where ProcessCommandLine has_all ('msdt', 'PCWDiagnostic', '-af', '.xml')
    | where InitiatingProcessFileName !~ 'pcwrun.exe'
    
